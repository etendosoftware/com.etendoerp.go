// GENERATED BY SCHEMA FORGE — DO NOT EDIT
// Generated: 2026-03-05T19:18:00.136Z
package com.etendoerp.go.process;

import java.util.Map;
import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
import org.openbravo.base.exception.OBException;
import org.openbravo.dal.service.OBDal;
import org.openbravo.erpCommon.utility.OBError;
import org.openbravo.scheduling.ProcessBundle;
import org.openbravo.service.db.DalBaseProcess;
import ;

/**
 * DAL Process: PostOrderProcess
 *
 * Executes precondition checks followed by process steps.
 */
public class PostOrderProcess extends DalBaseProcess {

  private static final Logger log = LogManager.getLogger();

  @Override
  protected void doExecute(ProcessBundle bundle) throws Exception {
    try {
      final Map<String, Object> params = bundle.getParams();
      final String recordId = (String) params.get("recordId");

      // Load entity
      final var order = OBDal.getInstance().get(.class, recordId);

      // --- Preconditions ---
      if (order.processed &#x3D;&#x3D; true) {
        throw new OBException("Order must be processed (completed or voided) before posting");
      }
      if (userPreferences.showAccounting &#x3D;&#x3D; true) {
        throw new OBException("User must have Show Accounting preference enabled");
      }

      // --- Process Steps ---
      // Step: 
      
      // Step: 
      
      // Step: 
      

      OBDal.getInstance().flush();

      final OBError msg = new OBError();
      msg.setType("Success");
      msg.setTitle("Process completed");
      bundle.setResult(msg);

    } catch (OBException e) {
      log.error("Process failed", e);
      final OBError msg = new OBError();
      msg.setType("Error");
      msg.setMessage(e.getMessage());
      bundle.setResult(msg);
      OBDal.getInstance().rollbackAndClose();
    }
  }
}
