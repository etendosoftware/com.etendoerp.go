<?xml version="1.0"?>
  <database name="FUNCTION ETGO_GET_TBAI_STATUS">
    <function name="ETGO_GET_TBAI_STATUS" type="VARCHAR" volatility="STABLE">
      <parameter name="p_c_invoice_id" type="VARCHAR" mode="in">
        <default/>
      </parameter>
      <body><![CDATA[v_estado character varying;
  v_ad_org_id character varying;
  v_date_invoiced timestamp without time zone;
  v_adoption_date timestamp without time zone;
BEGIN
  -- TOTAL FUNCTION. Refresh_Mode = 'S' runs this inside the business transaction,
  -- so ANY exception raised here rolls back the invoice save itself. Every edge
  -- case below must therefore resolve to a value, never to an error.

  -- Edge case 1 - null/blank target id. Cannot happen through the engine (it
  -- always passes a PK), but a NULL argument must not propagate as a NULL-handling
  -- surprise: answer the "no submission" state explicitly.
  IF p_c_invoice_id IS NULL THEN
    RETURN 'Pendiente';
  END IF;

  -- ADOPTION-DATE GATE (ETP-5216 follow-up). An invoice dated before the
  -- organization joined TicketBAI/Batuz is never submitted, so reporting it as
  -- 'Pendiente' states something false: it is not pending, it does not apply.
  -- This gate used to live in the React cell (`isSifEligibleByDate`, ETP-5122),
  -- which left the backend filter unable to see it - filtering by 'Pendiente'
  -- returned rows the grid then rendered as a dash. Worse, the cell compared
  -- every row against the adoption date of the SELECTED organization rather
  -- than the invoice's own, so a list spanning organizations measured them all
  -- with the wrong yardstick. Deciding it here makes the stored value the one
  -- truth for cell, filter and sort alike.
  SELECT i.ad_org_id, i.dateinvoiced
    INTO v_ad_org_id, v_date_invoiced
    FROM c_invoice i
   WHERE i.c_invoice_id = p_c_invoice_id;

  -- Edge case - the invoice row is gone (deleted between enqueue and recompute).
  -- Nothing to gate on; answer the "no submission" state rather than raising.
  IF v_ad_org_id IS NULL THEN
    RETURN 'Pendiente';
  END IF;

  -- Matched on the invoice's OWN organization, exactly (no org-tree walk), which
  -- is what Classic's TBAI_ExistConfigAndIsAvailable auxiliary input does.
  --
  -- ETP-5229: the gate is the EARLIEST-EVER config cutover DATE for the org,
  -- across ALL tbai_config rows - active or not - not just the currently
  -- active one. An org can deactivate an old config and activate a newer one
  -- (e.g. a provider migration or a Batuz territory change); an invoice dated
  -- between the OLD cutover and the NEW one was genuinely submitted under the
  -- old config and must keep showing its real historical status, not regress
  -- to 'NoAplica' because the config that covered it is no longer active.
  -- MIN() is a total aggregate: zero rows (no config ever) or all-NULL
  -- tbaisystemdate values both correctly yield NULL, handled by the
  -- IS NULL check right below - no separate empty-set branch needed.
  SELECT MIN(c.tbaisystemdate)
    INTO v_adoption_date
    FROM tbai_config c
   WHERE c.ad_org_id = v_ad_org_id;

  -- No config at all (active or not): the organization never joined, so no
  -- invoice of it applies. The dependency on tbai_config recomputes these the
  -- moment ANY config row for the org is inserted, updated (tbaisystemdate or
  -- isactive) or deleted, so both "org joins" and "org's earliest config
  -- changes" are picked up automatically.
  IF v_adoption_date IS NULL THEN
    RETURN 'NoAplica';
  END IF;

  -- A null invoice date cannot be proven eligible, so it is not. Same fail-safe
  -- direction the React gate took (`parseCalendarDate` returning null -> false).
  -- The comparison itself is NOT truncated to a day: Classic compares
  -- TO_TIMESTAMP(DateInvoiced) >= conf.tbaisystemdate, so a config carrying a
  -- time-of-day excludes invoices dated that same calendar day. Truncating here
  -- would silently disagree with Classic on exactly those rows.
  IF v_date_invoiced IS NULL OR v_date_invoiced < v_adoption_date THEN
    RETURN 'NoAplica';
  END IF;

  -- Edge case 2 - invoice with NO row in tbai_syncinvoice (never submitted, or
  -- TicketBAI simply not used for this invoice). SELECT ... INTO in PL/pgSQL
  -- assigns NULL for zero rows and does NOT raise (unlike SELECT ... INTO STRICT,
  -- which raises NO_DATA_FOUND - deliberately not used here).
  -- Edge case 3 - SEVERAL rows, including several sharing the same `created`
  -- timestamp. LIMIT 1 makes the result single-valued no matter how many rows
  -- match, so the multi-row case cannot raise TOO_MANY_ROWS. The tie-break on
  -- tbai_syncinvoice_id makes the choice DETERMINISTIC rather than arbitrary,
  -- which matters because the engine writes the value unconditionally on every
  -- recompute: without it, two recomputes of unchanged data could store different
  -- values and ad_scd_check would report permanent phantom drift.
  SELECT s.estado
    INTO v_estado
    FROM tbai_syncinvoice s
   WHERE s.c_invoice_id = p_c_invoice_id
   ORDER BY s.created DESC, s.tbai_syncinvoice_id DESC
   LIMIT 1;

  -- Edge case 4 - a row exists but ESTADO is NULL (the column is nullable, and a
  -- row is created before the response is parsed - see SynchronizeUtils.java:374-385,
  -- which builds the row first and sets ESTADO only in determineInvoiceState).
  -- Same answer as "no row": submission not resolved yet.
  IF v_estado IS NULL OR btrim(v_estado) = '' THEN
    RETURN 'Pendiente';
  END IF;

  -- Edge case 5 - an UNEXPECTED status value (TicketBAI adds a new state, or a
  -- tenant has legacy data). Pass it through UNCHANGED rather than mapping it to
  -- a known state or raising. The frontend badge already degrades gracefully:
  -- FiscalStatusBadge.jsx renders any unmapped value as its raw text with a
  -- neutral tone. Normalising here would invent information; raising would block
  -- the save. Passing through is the only option that neither lies nor breaks.
  RETURN v_estado;

-- Belt and braces. Nothing above should be able to raise, but an unforeseen
-- runtime error (a type change in a future TicketBAI version, say) must not take
-- the invoice down with it. Degrading to the "unknown" state keeps writes working;
-- the wrong badge is visible and recoverable, a failed save is not.
EXCEPTION
  WHEN OTHERS THEN
    RETURN 'Pendiente';
END ETGO_GET_TBAI_STATUS
]]></body>
    </function>
  </database>
